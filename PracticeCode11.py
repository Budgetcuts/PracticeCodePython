import Controller #controller is a placeholder for a class we're going to make

def main():
    done = False
    while(not Done):
        for e in events:
            if e == someevent:
                update models
            elif e == someotherevent:
                updatemodels
        process data
        redraw screen
main()
